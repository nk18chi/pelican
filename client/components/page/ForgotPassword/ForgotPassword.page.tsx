import { ForgotPasswordNextPageProps } from 'pages/forgot-password';
import ForgotPassword from './ForgotPassword';

const ForgotPasswordPage = (props: ForgotPasswordNextPageProps) => {
  return <ForgotPassword {...props} />;
};

export default ForgotPasswordPage;
