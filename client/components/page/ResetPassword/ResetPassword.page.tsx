import { ResetPasswordNextPageProps } from 'pages/reset-password';
import ResetPassword from './ResetPassword';

const ResetPasswordPage = (props: ResetPasswordNextPageProps) => {
  return <ResetPassword {...props} />;
};

export default ResetPasswordPage;
