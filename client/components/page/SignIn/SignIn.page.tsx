import { SignInNextPageProps } from 'pages/signin';
import SignIn from './SignIn';

const SignInPage = (props: SignInNextPageProps) => {
  return <SignIn {...props} />;
};

export default SignInPage;
